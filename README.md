# Terminal 1 — Backend
cd backend,npm install&npm run dev

# Terminal 2 — AI Service
cd ai-service,pip install google-genai,pip install google-genai
python app.py && pip install -r requirements.txt && python app.py(GEMINI_API_KEY=your_gemini_key_here) and there is changes in both .py service files.

# Terminal 3 — Frontend
cd frontend,npm install&npm run dev
